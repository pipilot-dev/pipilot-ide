---
name: use-pnpm
description: User prefers pnpm over npm for package management
type: feedback
---

Always use pnpm instead of npm for installing packages and running scripts.

**Why:** User explicitly corrected from npm to pnpm.
**How to apply:** Use `pnpm install`, `pnpm dev`, `pnpm add`, etc. instead of npm equivalents.
